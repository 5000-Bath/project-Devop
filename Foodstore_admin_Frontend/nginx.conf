server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # ✅ แยกการ proxy ตาม HTTP method
  location /api/ {
    # ถ้าเป็นคำสั่งเขียน (POST, PUT, PATCH, DELETE) → ชี้ไป backend ระยอง
    if ($request_method ~* ^(POST|PUT|PATCH|DELETE)$) {
      proxy_pass http://10.10.10.8:31000;  # 👈 Backend ระยอง (Master)
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-Proto $scheme;
      break;
    }

    # ถ้าเป็น GET, HEAD → ใช้ backend ภายใน (ศาลายา)
    proxy_pass http://backend:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
  }

  # ✅ รองรับ React SPA (refresh ไม่ 404)
  location / {
    try_files $uri /index.html;
  }
}
