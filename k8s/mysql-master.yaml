apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql-rayong
  namespace: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql-rayong
  template:
    metadata:
      labels:
        app: mysql-rayong
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_ROOT_PASSWORD
        - name: MYSQL_DATABASE
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_DATABASE
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_USER
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: MYSQL_PASSWORD
        volumeMounts:
        - name: mysql-storage
          mountPath: /var/lib/mysql
        - name: mysql-init
          mountPath: /docker-entrypoint-initdb.d
        - name: mysql-config
          mountPath: /etc/mysql/conf.d
      volumes:
      - name: mysql-storage
        persistentVolumeClaim:
          claimName: mysql-pvc
      - name: mysql-init
        configMap:
          name: mysql-init-master
      - name: mysql-config
        configMap:
          name: mysql-config-master
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-rayong
  namespace: production
spec:
  selector:
    app: mysql-rayong
  ports:
    - port: 3306
      targetPort: 3306
  type: ClusterIP
